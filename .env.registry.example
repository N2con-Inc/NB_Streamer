# Container Registry Configuration
# Copy this to .env.registry and customize for your setup

# Choose your registry (ghcr.io, docker.io, or custom registry)
IMAGE_REGISTRY=ghcr.io

# Your registry namespace (GitHub username, Docker Hub username, etc.)
IMAGE_NAMESPACE=your-username

# Image name (usually matches repository name)
IMAGE_NAME=nb-streamer

# Default image tag to deploy
IMAGE_TAG=latest

# GitHub Container Registry (if using ghcr.io)
GITHUB_USERNAME=your-github-username
GITHUB_TOKEN=your-personal-access-token

# Docker Hub (if using docker.io)
DOCKERHUB_USERNAME=your-dockerhub-username
DOCKERHUB_TOKEN=your-dockerhub-token

# Custom registry (if using private registry)
REGISTRY_USERNAME=your-username
REGISTRY_PASSWORD=your-password
