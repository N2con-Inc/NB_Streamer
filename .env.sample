# NB_Streamer Configuration
# Copy this file to .env and customize for your environment

# Server Configuration
NB_HOST=0.0.0.0
NB_PORT=8000
NB_DEBUG=false

# Graylog Configuration
# Update these with your actual Graylog server details
NB_GRAYLOG_HOST=10.0.1.244
NB_GRAYLOG_PORT=12201
NB_GRAYLOG_PROTOCOL=udp

# Tenant/Client Configuration - REQUIRED
# This identifies which tenant/client this instance serves
# All GELF messages will include this as _NB_tenant field
# Example: "n2con", "production_client", "dev_environment"
NB_TENANT_ID=n2con

# Authentication Configuration - REQUIRED
# Choose one authentication method:

# Option 1: Bearer Token (Recommended)
NB_AUTH_TYPE=bearer
NB_AUTH_TOKEN=NB_STREAMER_SECURE_TOKEN_CHANGE_ME

# Option 2: Basic Authentication
# NB_AUTH_TYPE=basic
# NB_AUTH_USERNAME=username
# NB_AUTH_PASSWORD=password

# Option 3: Custom Header
# NB_AUTH_TYPE=header
# NB_AUTH_HEADER_NAME=X-Custom-Auth
# NB_AUTH_HEADER_VALUE=custom-value

# Message Configuration
NB_COMPRESSION_ENABLED=true
NB_MAX_MESSAGE_SIZE=8192

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR
NB_LOG_LEVEL=INFO

# Production Notes:
# 1. Generate a strong, unique token for NB_AUTH_TOKEN
# 2. Use your actual Graylog server IP for NB_GRAYLOG_HOST
# 3. Set NB_TENANT_ID to identify your organization/environment
# 4. Consider setting NB_LOG_LEVEL=INFO for production (less verbose)
# 5. Ensure NB_GRAYLOG_HOST is reachable from your deployment environment
